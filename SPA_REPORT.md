# Hansen SPA Report

**Classification: INSUFFICIENT_HISTORY**

No SPA statistic or p-value is reported. A valid SPA test requires a settled OOS loss-differential matrix for production and multiple preregistered candidates. The uploaded database does not contain that matrix. Registry fields are present and promotion status is `NOT_PROMOTED_INSUFFICIENT_SETTLED_HISTORY`.
