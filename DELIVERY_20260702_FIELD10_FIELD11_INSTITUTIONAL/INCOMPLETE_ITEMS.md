# Incomplete or Unvalidated Items

The following are intentionally disclosed and are **not** claimed as complete:

1. **Field 10 LambdaMART/expected-pips production ML** — no leakage-safe mature training set or validated model artifacts were supplied; LightGBM/CatBoost models were not trained or promoted.
2. **Field 10 exact new table/column specification** — the existing immutable Field 10 table remains authoritative; the full new expected-pips/scenario column set was not fabricated.
3. **Timestamped historical regime/sentiment/news/liquidity/cross-market archives** — not consistently available. Field 11 assigns neutral/UNAVAILABLE component evidence and caps reliability rather than backfilling current values.
4. **True STUMPY Matrix Profile backend** — no new heavy dependency was added. The persisted 12-bar normalized shape vector plus vector shortlist acts as the motif index; engine labels share the bounded hybrid implementation.
5. **Engine-specific full behavior** — Matrix Profile, DTW, KNN, Regime-First and Shape-First labels do not yet dispatch to completely independent backends. Constrained DTW is always used on the shortlisted candidates.
6. **Formal conformal coverage** — Field 11 uses empirical 50%/80% weighted bands and explicitly labels them historical empirical only.
7. **Historical bias/regime-switch frequencies** — unavailable without timestamped regime/bias archives and therefore not invented.
8. **Live 25-day Field 11 validation** — persistence and settlement are tested with deterministic fixtures; live mature outcomes do not yet exist.
9. **Monolithic pytest process** — stalls at 70% from accumulated legacy process state; all 295 tests pass in isolated segments.
10. **Exact Python 3.12 test** — deployment target is 3.12, but validation container used Python 3.13.5.
11. **Physical iPhone 11 Pro test** — automated mobile structure and startup passed; no physical-device run was available.
12. **Streamlit Cloud durable disk** — local SQLite/Parquet persistence is not guaranteed across platform replacement unless external backup/storage is configured.
